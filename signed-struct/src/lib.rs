/// Library for code that will be use by both Pyrsia nodes and Pyrsia clients.

pub mod signed;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4
        );
    }
}
